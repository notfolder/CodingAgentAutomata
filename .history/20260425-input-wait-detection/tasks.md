# 実装タスク一覧（TTY入力待機検知 / Group Webhook / LLM設定検証）

## T-01 consumer実行環境をDinD前提へ変更

- 内容
  - 実装着手前に docs/ebpf-dind-investigation.md を全文参照し、実験条件と矛盾がないことを確認する
  - consumerコンテナ実行条件をDinD前提に変更する
  - cli-exec起動経路と権限設定が eBPF 要件を満たすことを確認する
- 完了条件
  - consumerから起動されるCLI実行経路で DinD が成立している
  - 実行条件が docs/ebpf-dind-investigation.md と矛盾しない
- バリデーションタスク
  - BTF存在、CAP_BPF/CAP_PERFMON確認を実行しログで確認する

## T-02 eBPF利用可否判定機能の実装

- 内容
  - BTF存在確認、権限確認、判定タイムアウトを実装する
  - 利用不可時はTTY検知を無効化して処理継続する
- 完了条件
  - BTF不足、権限不足、タイムアウトの各ケースで期待どおり分岐する
- バリデーションタスク
  - 単体テストで3パターン（不足/不足/タイムアウト）を通す

## T-03 Tracee起動・TTY待機検知機能の実装

- 内容
  - Tracee起動・停止管理を実装する
  - read(2) on TTY の待機検知判定を実装する
- 完了条件
  - TTY待機を再現したときに検知イベントを取得できる
- バリデーションタスク
  - 擬似TTY待機シナリオで検知ログを確認する

## T-04 TTY検知時のCLI強制終了と失敗報告の実装

- 内容
  - 検知時にCLIプロセスを強制終了する
  - GitLabコメント投稿と tasks.error_message / tasks.cli_log 更新を行う
- 完了条件
  - TTY待機発生時に failed へ遷移し、通知と履歴記録が完了する
- バリデーションタスク
  - 結合テストでコメント本文、error_message、cli_log を検証する

## T-05 git config自動設定処理の実装

- 内容
  - CLIコンテナ起動直後に git user.name / user.email を設定する
- 完了条件
  - user.name が GITLAB_BOT_NAME、user.email が GITLAB_BOT_NAME@localhost になる
- バリデーションタスク
  - コンテナ内 git config 値を検証する

## T-06 Group Webhook受信標準化

- 内容
  - Webhook受信起点を Group Webhook 標準に変更する
  - Project Webhook 固定前提の処理を除去する
- 完了条件
  - Group配下プロジェクトイベントが単一Webhookで受信される
- バリデーションタスク
  - 複数プロジェクトからの受信テストを実施する

## T-07 Idempotency-Keyによる重複受信抑止実装

- 内容
  - 受信時に Idempotency-Key を使った重複判定を行う
  - 重複イベントは再処理せず受付のみ行う
- 完了条件
  - 同一キー再送時に二重タスク投入されない
- バリデーションタスク
  - 同一イベント複数送信で投入件数が1件であることを検証する

## T-08 Webhook失敗時運用（警告ログ + GitLab再送前提）実装

- 内容
  - 受信失敗をWARNINGログに統一する
  - 本システム側でWebhook再送処理を追加しない
- 完了条件
  - 失敗時にWARNING記録され、再送はGitLab機能に委譲される
- バリデーションタスク
  - 受信失敗ケースでログ内容とHTTP応答を検証する

## T-09 LLMキー保存時バリデーション実装

- 内容
  - ユーザー設定保存時にキー妥当性チェックを実行する
  - 失敗時は保存せずGUIエラー返却する
- 完了条件
  - 無効キーが保存されない
- バリデーションタスク
  - 正常キー/異常キー双方のAPIテストを実施する

## T-10 モデル候補表示と手入力許可の実装

- 内容
  - モデル候補取得APIを実装する
  - 候補取得失敗時も手入力保存を許可する
- 完了条件
  - 候補成功時は表示され、失敗時も保存継続できる
- バリデーションタスク
  - 候補取得成功/失敗の両ケースでGUI動作を検証する

## T-11 プロンプトテンプレート必須指示反映

- 内容
  - F-3/F-4テンプレート運用に必須指示（問い合わせ禁止、TTY待機時failed、git_clone_pathなど）を反映する
- 完了条件
  - 生成プロンプトに必須指示が常に含まれる
- バリデーションタスク
  - プロンプト生成結果のスナップショット検証を実施する

## T-12 共通化・重複排除リファクタリング

- 内容
  - eBPF判定、TTY判定、キー検証、Webhook重複判定を共通化する
- 完了条件
  - 同一意味の処理が複数箇所に再実装されていない
- バリデーションタスク
  - 静的レビューで重複実装がないことを確認する

## T-13 単体テスト実装

- 内容
  - 変更クラスすべての単体テストを追加する
- 完了条件
  - 対象モジュールの単体テストが全件成功する
- バリデーションタスク
  - テスト実行レポートで失敗0件を確認する

## T-14 結合テスト実装

- 内容
  - producer-consumer、consumer-gitlab、backend-frontendの結合テストを追加する
- 完了条件
  - 変更要件1〜3をまたぐ連携テストが成功する
- バリデーションタスク
  - 主要シナリオ（TTY検知、Group Webhook、LLM保存）を通す

## T-15 E2Eテスト実装・実行

- 内容
  - 変更要件に対応するPlaywrightシナリオを実装する
  - docker compose --profile test で test_playwright 内実行を行う
- 完了条件
  - 追加E2Eと既存主要E2Eがすべて成功する
- バリデーションタスク
  - 実行結果レポートで全件成功を確認する

## T-16 ログ・運用手順更新

- 内容
  - TTY検知ログ、Webhook受信ログ、LLM検証ログの運用確認手順を整備する
- 完了条件
  - 障害時の一次対応手順が手順書として再現可能である
- バリデーションタスク
  - 想定障害3ケースで手順どおり確認できることをレビューする

## T-17 変更点実装完了確認（総合）

- 内容
  - 変更要件定義書と変更詳細設計書の全項目をチェックリストで照合する
- 完了条件
  - 要件・設計の未実装項目が0件である
- バリデーションタスク
  - チェックリストにレビュー記録を残し、承認を得る

## T-18 既存ドキュメント反映

- 内容
  - 今回の変更要件・変更設計を docs/requirements.md と docs/detail_design.md に反映する
- 完了条件
  - 2文書が最新実装と一致し、矛盾がない
- バリデーションタスク
  - 差分レビューで更新漏れがないことを確認する
